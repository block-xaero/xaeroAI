```json
[
  {
    "instruction": "Create a class diagram showing the trait object pattern for a drawable graphics system with Rectangle and Circle implementations",
    "output": "```mermaid\nclassDiagram\n    class Drawable {\n        <<interface>>\n        +draw(&self)\n    }\n    \n    class Rectangle {\n        +width: f64\n        +height: f64\n        +new(width: f64, height: f64) Rectangle\n        +draw(&self)\n    }\n    \n    class Circle {\n        +radius: f64\n        +new(radius: f64) Circle\n        +draw(&self)\n    }\n    \n    class Canvas {\n        +shapes: Vec~Box~dyn Drawable~~\n        +add_shape(shape: Box~dyn Drawable~)\n        +render(&self)\n    }\n    \n    Drawable <|-- Rectangle : impl\n    Drawable <|-- Circle : impl\n    Canvas --> Drawable : uses\n```\n\n```rust\ntrait Drawable {\n    fn draw(&self);\n}\n\nstruct Rectangle {\n    width: f64,\n    height: f64,\n}\n\nimpl Rectangle {\n    fn new(width: f64, height: f64) -> Self {\n        Rectangle { width, height }\n    }\n}\n\nimpl Drawable for Rectangle {\n    fn draw(&self) {\n        println!(\"Drawing rectangle {}x{}\", self.width, self.height);\n    }\n}\n\nstruct Circle {\n    radius: f64,\n}\n\nimpl Circle {\n    fn new(radius: f64) -> Self {\n        Circle { radius }\n    }\n}\n\nimpl Drawable for Circle {\n    fn draw(&self) {\n        println!(\"Drawing circle with radius {}\", self.radius);\n    }\n}\n\nstruct Canvas {\n    shapes: Vec<Box<dyn Drawable>>,\n}\n\nimpl Canvas {\n    fn add_shape(&mut self, shape: Box<dyn Drawable>) {\n        self.shapes.push(shape);\n    }\n    \n    fn render(&self) {\n        for shape in &self.shapes {\n            shape.draw();\n        }\n    }\n}\n```"
  },
  {
    "instruction": "Show a class diagram for the newtype pattern wrapping a user ID with validation",
    "output": "```mermaid\nclassDiagram\n    class UserId {\n        -id: String\n        +new(id: String) Result~UserId, ValidationError~\n        +as_str(&self) &str\n        +into_inner(self) String\n    }\n    \n    class ValidationError {\n        +message: String\n        +new(message: String) ValidationError\n    }\n    \n    class User {\n        +id: UserId\n        +name: String\n        +email: String\n        +new(id: UserId, name: String, email: String) User\n    }\n    \n    User --> UserId : contains\n    UserId --> ValidationError : creates\n```\n\n```rust\nuse std::fmt;\n\n#[derive(Debug, Clone, PartialEq, Eq, Hash)]\nstruct UserId(String);\n\n#[derive(Debug)]\nstruct ValidationError {\n    message: String,\n}\n\nimpl ValidationError {\n    fn new(message: String) -> Self {\n        ValidationError { message }\n    }\n}\n\nimpl fmt::Display for ValidationError {\n    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {\n        write!(f, \"Validation error: {}\", self.message)\n    }\n}\n\nimpl std::error::Error for ValidationError {}\n\nimpl UserId {\n    fn new(id: String) -> Result<Self, ValidationError> {\n        if id.is_empty() {\n            return Err(ValidationError::new(\"User ID cannot be empty\".to_string()));\n        }\n        if id.len() > 50 {\n            return Err(ValidationError::new(\"User ID too long\".to_string()));\n        }\n        Ok(UserId(id))\n    }\n    \n    fn as_str(&self) -> &str {\n        &self.0\n    }\n    \n    fn into_inner(self) -> String {\n        self.0\n    }\n}\n\nstruct User {\n    id: UserId,\n    name: String,\n    email: String,\n}\n\nimpl User {\n    fn new(id: UserId, name: String, email: String) -> Self {\n        User { id, name, email }\n    }\n}\n```"
  },
  {
    "instruction": "Design a class diagram for a typestate builder pattern for HTTP request configuration",
    "output": "```mermaid\nclassDiagram\n    class HttpRequestBuilder~T~ {\n        +url: Option~String~\n        +method: Option~String~\n        +headers: HashMap~String, String~\n        +body: Option~String~\n        +new() HttpRequestBuilder~NoUrl~\n        +url(self, url: String) HttpRequestBuilder~HasUrl~\n        +method(self, method: String) Self\n        +header(self, key: String, value: String) Self\n        +body(self, body: String) Self\n    }\n    \n    class NoUrl {\n        <<phantom>>\n    }\n    \n    class HasUrl {\n        <<phantom>>\n    }\n    \n    class HttpRequest {\n        +url: String\n        +method: String\n        +headers: HashMap~String, String~\n        +body: Option~String~\n    }\n    \n    HttpRequestBuilder --> NoUrl : initial state\n    HttpRequestBuilder --> HasUrl : after url set\n    HttpRequestBuilder --> HttpRequest : build()\n```\n\n```rust\nuse std::collections::HashMap;\nuse std::marker::PhantomData;\n\n// Type states\nstruct NoUrl;\nstruct HasUrl;\n\nstruct HttpRequestBuilder<T> {\n    url: Option<String>,\n    method: Option<String>,\n    headers: HashMap<String, String>,\n    body: Option<String>,\n    _state: PhantomData<T>,\n}\n\nimpl HttpRequestBuilder<NoUrl> {\n    fn new() -> Self {\n        HttpRequestBuilder {\n            url: None,\n            method: None,\n            headers: HashMap::new(),\n            body: None,\n            _state: PhantomData,\n        }\n    }\n    \n    fn url(mut self, url: String) -> HttpRequestBuilder<HasUrl> {\n        self.url = Some(url);\n        HttpRequestBuilder {\n            url: self.url,\n            method: self.method,\n            headers: self.headers,\n            body: self.body,\n            _state: PhantomData,\n        }\n    }\n}\n\nimpl<T> HttpRequestBuilder<T> {\n    fn method(mut self, method: String) -> Self {\n        self.method = Some(method);\n        self\n    }\n    \n    fn header(mut self, key: String, value: String) -> Self {\n        self.headers.insert(key, value);\n        self\n    }\n    \n    fn body(mut self, body: String) -> Self {\n        self.body = Some(body);\n        self\n    }\n}\n\nimpl HttpRequestBuilder<HasUrl> {\n    fn build(self) -> HttpRequest {\n        HttpRequest {\n            url: self.url.unwrap(),\n            method: self.method.unwrap_or_else(|| \"GET\".to_string()),\n            headers: self.headers,\n            body: self.body,\n        }\n    }\n}\n\nstruct HttpRequest {\n    url: String,\n    method: String,\n    headers: HashMap<String, String>,\n    body: Option<String>,\n}\n```"
  },
  {
    "instruction": "Create a class diagram showing Result-based error handling for a file processing system",
    "output": "```mermaid\nclassDiagram\n    class FileProcessor {\n        +process_file(path: &str) Result~ProcessedData, ProcessError~\n        +read_file(path: &str) Result~String, ProcessError~\n        +validate_content(content: &str) Result~(), ProcessError~\n        +transform_data(content: String) Result~ProcessedData, ProcessError~\n    }\n    \n    class ProcessError {\n        <<interface>>\n        +description(&self) &str\n    }\n    \n    class IoError {\n        +kind: String\n        +message: String\n    }\n    \n    class ValidationError {\n        +field: String\n        +message: String\n    }\n    \n    class TransformError {\n        +reason: String\n    }\n    \n    class ProcessedData {\n        +content: String\n        +metadata: HashMap~String, String~\n        +timestamp: SystemTime\n    }\n    \n    ProcessError <|-- IoError : impl\n    ProcessError <|-- ValidationError : impl\n    ProcessError <|-- TransformError : impl\n    FileProcessor --> ProcessError : returns\n    FileProcessor --> ProcessedData : returns\n```\n\n```rust\nuse std::collections::HashMap;\nuse std::fmt;\nuse std::time::SystemTime;\n\n#[derive(Debug)]\nenum ProcessError {\n    Io(IoError),\n    Validation(ValidationError),\n    Transform(TransformError),\n}\n\n#[derive(Debug)]\nstruct IoError {\n    kind: String,\n    message: String,\n}\n\n#[derive(Debug)]\nstruct ValidationError {\n    field: String,\n    message: String,\n}\n\n#[derive(Debug)]\nstruct TransformError {\n    reason: String,\n}\n\nimpl fmt::Display for ProcessError {\n    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {\n        match self {\n            ProcessError::Io(e) => write!(f, \"IO Error: {} - {}\", e.kind, e.message),\n            ProcessError::Validation(e) => write!(f, \"Validation Error in {}: {}\", e.field, e.message),\n            ProcessError::Transform(e) => write!(f, \"Transform Error: {}\", e.reason),\n        }\n    }\n}\n\nimpl std::error::Error for ProcessError {}\n\nstruct ProcessedData {\n    content: String,\n    metadata: HashMap<String, String>,\n    timestamp: SystemTime,\n}\n\nstruct FileProcessor;\n\nimpl FileProcessor {\n    fn process_file(path: &str) -> Result<ProcessedData, ProcessError> {\n        let content = Self::read_file(path)?;\n        Self::validate_content(&content)?;\n        Self::transform_data(content)\n    }\n    \n    fn read_file(path: &str) -> Result<String, ProcessError> {\n        std::fs::read_to_string(path)\n            .map_err(|e| ProcessError::Io(IoError {\n                kind: \"FileRead\".to_string(),\n                message: e.to_string(),\n            }))\n    }\n    \n    fn validate_content(content: &str) -> Result<(), ProcessError> {\n        if content.is_empty() {\n            return Err(ProcessError::Validation(ValidationError {\n                field: \"content\".to_string(),\n                message: \"File cannot be empty\".to_string(),\n            }));\n        }\n        Ok(())\n    }\n    \n    fn transform_data(content: String) -> Result<ProcessedData, ProcessError> {\n        let mut metadata = HashMap::new();\n        metadata.insert(\"size\".to_string(), content.len().to_string());\n        \n        Ok(ProcessedData {\n            content,\n            metadata,\n            timestamp: SystemTime::now(),\n        })\n    }\n}\n```"
  },
  {
    "instruction": "Show a class diagram for an actor model implementation using channels for message passing",
    "output": "```mermaid\nclassDiagram\n    class Actor~M~ {\n        +receiver: Receiver~M~\n        +run(&mut self)\n        +handle_message(&mut self, message: M)\n    }\n    \n    class ActorHandle~M~ {\n        +sender: Sender~M~\n        +send(&self, message: M) Result~(), SendError~\n    }\n    \n    class CounterActor {\n        +count: i32\n        +receiver: Receiver~CounterMessage~\n        +new(receiver: Receiver~CounterMessage~) CounterActor\n        +handle_message(&mut self, message: CounterMessage)\n    }\n    \n    class CounterMessage {\n        <<enumeration>>\n        +Increment\n        +Decrement\n        +GetCount(Sender~i32~)\n        +Stop\n    }\n    \n    class ActorSystem {\n        +spawn_counter_actor() ActorHandle~CounterMessage~\n    }\n    \n    Actor <|-- CounterActor : impl\n    ActorSystem --> ActorHandle : creates\n    ActorHandle --> CounterMessage : sends\n    CounterActor --> CounterMessage : receives\n```\n\n```rust\nuse std::sync::mpsc::{self, Receiver, Sender, SendError};\nuse std::thread;\n\ntrait Actor<M> {\n    fn run(&mut self);\n    fn handle_message(&mut self, message: M);\n}\n\nstruct ActorHandle<M> {\n    sender: Sender<M>,\n}\n\nimpl<M> ActorHandle<M> {\n    fn send(&self, message: M) -> Result<(), SendError<M>> {\n        self.sender.send(message)\n    }\n}\n\n#[derive(Debug)]\nenum CounterMessage {\n    Increment,\n    Decrement,\n    GetCount(Sender<i32>),\n    Stop,\n}\n\nstruct CounterActor {\n    count: i32,\n    receiver: Receiver<CounterMessage>,\n}\n\nimpl CounterActor {\n    fn new(receiver: Receiver<CounterMessage>) -> Self {\n        CounterActor {\n            count: 0,\n            receiver,\n        }\n    }\n}\n\nimpl Actor<CounterMessage> for CounterActor {\n    fn run(&mut self) {\n        while let Ok(message) = self.receiver.recv() {\n            match message {\n                CounterMessage::Stop => break,\n                msg => self.handle_message(msg),\n            }\n        }\n    }\n    \n    fn handle_message(&mut self, message: CounterMessage) {\n        match message {\n            CounterMessage::Increment => self.count += 1,\n            CounterMessage::Decrement => self.count -= 1,\n            CounterMessage::GetCount(reply_to) => {\n                let _ = reply_to.send(self.count);\n            }\n            CounterMessage::Stop => {} // Handled in run()\n        }\n    }\n}\n\nstruct ActorSystem;\n\nimpl ActorSystem {\n    fn spawn_counter_actor() -> ActorHandle<CounterMessage> {\n        let (sender, receiver) = mpsc::channel();\n        let mut actor = CounterActor::new(receiver);\n        \n        thread::spawn(move || {\n            actor.run();\n        });\n        \n        ActorHandle { sender }\n    }\n}\n```"
  },
  {
    "instruction": "Design a class diagram for a channel-based observer pattern for event notifications",
    "output": "```mermaid\nclassDiagram\n    class EventPublisher~T~ {\n        +subscribers: Vec~Sender~T~~\n        +subscribe(&mut self) Receiver~T~\n        +unsubscribe(&mut self, subscriber_id: usize)\n        +publish(&self, event: T)\n    }\n    \n    class EventSubscriber~T~ {\n        +receiver: Receiver~T~\n        +id: usize\n        +new(receiver: Receiver~T~, id: usize) EventSubscriber~T~\n        +listen(&self)\n    }\n    \n    class UserEvent {\n        <<enumeration>>\n        +Login(String)\n        +Logout(String)\n        +ProfileUpdate(String, String)\n    }\n    \n    class EmailNotifier {\n        +receiver: Receiver~UserEvent~\n        +new(receiver: Receiver~UserEvent~) EmailNotifier\n        +handle_event(&self, event: UserEvent)\n    }\n    \n    class LoggingService {\n        +receiver: Receiver~UserEvent~\n        +new(receiver: Receiver~UserEvent~) LoggingService\n        +handle_event(&self, event: UserEvent)\n    }\n    \n    EventPublisher --> UserEvent : publishes\n    EventSubscriber --> UserEvent : receives\n    EmailNotifier --> UserEvent : handles\n    LoggingService --> UserEvent : handles\n```\n\n```rust\nuse std::sync::mpsc::{self, Receiver, Sender};\nuse std::thread;\n\n#[derive(Clone, Debug)]\nenum UserEvent {\n    Login(String),\n    Logout(String),\n    ProfileUpdate(String, String), // user_id, field_name\n}\n\nstruct EventPublisher<T: Clone> {\n    subscribers: Vec<Sender<T>>,\n}\n\nimpl<T: Clone> EventPublisher<T> {\n    fn new() -> Self {\n        EventPublisher {\n            subscribers: Vec::new(),\n        }\n    }\n    \n    fn subscribe(&mut self) -> Receiver<T> {\n        let (sender, receiver) = mpsc::channel();\n        self.subscribers.push(sender);\n        receiver\n    }\n    \n    fn publish(&self, event: T) {\n        self.subscribers.retain(|sender| {\n            sender.send(event.clone()).is_ok()\n        });\n    }\n}\n\nstruct EventSubscriber<T> {\n    receiver: Receiver<T>,\n    id: usize,\n}\n\nimpl<T> EventSubscriber<T> {\n    fn new(receiver: Receiver<T>, id: usize) -> Self {\n        EventSubscriber { receiver, id }\n    }\n    \n    fn listen<F>(&self, mut handler: F)\n    where\n        F: FnMut(T),\n    {\n        while let Ok(event) = self.receiver.recv() {\n            handler(event);\n        }\n    }\n}\n\nstruct EmailNotifier {\n    receiver: Receiver<UserEvent>,\n}\n\nimpl EmailNotifier {\n    fn new(receiver: Receiver<UserEvent>) -> Self {\n        EmailNotifier { receiver }\n    }\n    \n    fn start(self) {\n        thread::spawn(move || {\n            while let Ok(event) = self.receiver.recv() {\n                self.handle_event(event);\n            }\n        });\n    }\n    \n    fn handle_event(&self, event: UserEvent) {\n        match event {\n            UserEvent::Login(user_id) => {\n                println!(\"Sending welcome email to user: {}\", user_id);\n            }\n            UserEvent::ProfileUpdate(user_id, field) => {\n                println!(\"Sending profile update confirmation to {}: {}\", user_id, field);\n            }\n            _ => {}\n        }\n    }\n}\n\nstruct LoggingService {\n    receiver: Receiver<UserEvent>,\n}\n\nimpl LoggingService {\n    fn new(receiver: Receiver<UserEvent>) -> Self {\n        LoggingService { receiver }\n    }\n    \n    fn start(self) {\n        thread::spawn(move || {\n            while let Ok(event) = self.receiver.recv() {\n                self.handle_event(event);\n            }\n        });\n    }\n    \n    fn handle_event(&self, event: UserEvent) {\n        println!(\"LOG: {:?}\", event);\n    }\n}\n```"
  },
  {
    "instruction": "Create a class diagram showing smart pointer usage with Box, Rc, and Arc for a tree data structure",
    "output": "```mermaid\nclassDiagram\n    class TreeNode~T~ {\n        +value: T\n        +children: Vec~Box~TreeNode~T~~~\n        +new(value: T) TreeNode~T~\n        +add_child(&mut self, child: TreeNode~T~)\n        +traverse(&self)\n    }\n    \n    class SharedTreeNode~T~ {\n        +value: T\n        +children: Vec~Rc~RefCell~SharedTreeNode~T~~~~\n        +parent: Option~Weak~RefCell~SharedTreeNode~T~~~~\n        +new(value: T) Rc~RefCell~SharedTreeNode~T~~~\n        +add_child(&mut self, child: Rc~RefCell~SharedTreeNode~T~~~)\n    }\n    \n    class ConcurrentTreeNode~T~ {\n        +value: T\n        +children: Vec~Arc~Mutex~ConcurrentTreeNode~T~~~~\n        +new(value: T) Arc~Mutex~ConcurrentTreeNode~T~~~\n        +add_child(&self, child: Arc~Mutex~ConcurrentTreeNode~T~~~)\n        +parallel_traverse(&self)\n    }\n    \n    class Box~T~ {\n        <<smart_pointer>>\n        +new(value: T) Box~T~\n        +leak(b: Box~T~) &'static mut T\n    }\n    \n    class Rc~T~ {\n        <<smart_pointer>>\n        +new(value: T) Rc~T~\n        +clone(&self) Rc~T~\n        +strong_count(&self) usize\n    }\n    \n    class Arc~T~ {\n        <<smart_pointer>>\n        +new(value: T) Arc~T~\n        +clone(&self) Arc~T~\n        +strong_count(&self) usize\n    }\n    \n    TreeNode --> Box : uses\n    SharedTreeNode --> Rc : uses\n    ConcurrentTreeNode --> Arc : uses\n```\n\n```rust\nuse std::sync::{Arc, Mutex};\nuse std::rc::{Rc, Weak};\nuse std::cell::RefCell;\nuse std::thread;\n\n// Owned tree using Box\nstruct TreeNode<T> {\n    value: T,\n    children: Vec<Box<TreeNode<T>>>,\n}\n\nimpl<T> TreeNode<T> {\n    fn new(value: T) -> Self {\n        TreeNode {\n            value,\n            children: Vec::new(),\n        }\n    }\n    \n    fn add_child(&mut self, child: TreeNode<T>) {\n        self.children.push(Box::new(child));\n    }\n    \n    fn traverse(&self)\n    where\n        T: std::fmt::Display,\n    {\n        println!(\"{}\", self.value);\n        for child in &self.children {\n            child.traverse();\n        }\n    }\n}\n\n// Shared tree using Rc for multiple ownership\nstruct SharedTreeNode<T> {\n    value: T,\n    children: Vec<Rc<RefCell<SharedTreeNode<T>>>>,\n    parent: Option<Weak<RefCell<SharedTreeNode<T>>>>,\n}\n\nimpl<T> SharedTreeNode<T> {\n    fn new(value: T) -> Rc<RefCell<Self>> {\n        Rc::new(RefCell::new(SharedTreeNode {\n            value,\n            children: Vec::new(),\n            parent: None,\n        }))\n    }\n    \n    fn add_child(\n        parent: &Rc<RefCell<SharedTreeNode<T>>>,\n        child: Rc<RefCell<SharedTreeNode<T>>>,\n    ) {\n        child.borrow_mut().parent = Some(Rc::downgrade(parent));\n        parent.borrow_mut().children.push(child);\n    }\n}\n\n// Thread-safe tree using Arc and Mutex\nstruct ConcurrentTreeNode<T> {\n    value: T,\n    children: Vec<Arc<Mutex<ConcurrentTreeNode<T>>>>,\n}\n\nimpl<T> ConcurrentTreeNode<T> {\n    fn new(value: T) -> Arc<Mutex<Self>> {\n        Arc::new(Mutex::new(ConcurrentTreeNode {\n            value,\n            children: Vec::new(),\n        }))\n    }\n    \n    fn add_child(\n        parent: &Arc<Mutex<ConcurrentTreeNode<T>>>,\n        child: Arc<Mutex<ConcurrentTreeNode<T>>>,\n    ) {\n        parent.lock().unwrap().children.push(child);\n    }\n    \n    fn parallel_traverse(node: Arc<Mutex<Self>>)\n    where\n        T: std::fmt::Display + Send + Sync + 'static,\n    {\n        let children = {\n            let locked = node.lock().unwrap();\n            println!(\"{}\", locked.value);\n            locked.children.clone()\n        };\n        \n        let handles: Vec<_> = children\n            .into_iter()\n            .map(|child| {\n                thread::spawn(move || {\n                    Self::parallel_traverse(child);\n                })\n            })\n            .collect();\n        \n        for handle in handles {\n            handle.join().unwrap();\n        }\n    }\n}\n```"
  },
  {
    "instruction": "Show a class diagram for interior mutability patterns using RefCell and Mutex in a cache system",
    "output": "```mermaid\nclassDiagram\n    class Cache~K, V~ {\n        <<interface>>\n        +get(&self, key: &K) Option~V~\n        +insert(&self, key: K, value: V)\n        +remove(&self, key: &K) Option~V~\n        +clear(&self)\n    }\n    \n    class SingleThreadCache~K, V~ {\n        +data: RefCell~HashMap~K, V~~\n        +max_size: usize\n        +new(max_size: usize) SingleThreadCache~K, V~\n    }\n    \n    class MultiThreadCache~K, V~ {\n        +data: Mutex~HashMap~K, V~~\n        +max_size: usize\n        +new(max_size: usize) MultiThreadCache~K, V~\n    }\n    \n    class RefCell~T~ {\n        <<interior_mutability>>\n        +new(value: T) RefCell~T~\n        +borrow(&self) Ref~T~\n        +borrow_mut(&self) RefMut~T~\n    }\n    \n    class Mutex~T~ {\n        <<interior_mutability>>\n        +new(value: T) Mutex~T~\n        +lock(&self) MutexGuard~T~\n    }\n    \n    class CacheStats {\n        +hits: RefCell~u64~\n        +misses: RefCell~u64~\n        +new() CacheStats\n        +record_hit(&self)\n        +record_miss(&self)\n        +get_stats(&self) (u64, u64)\n    }\n    \n    Cache <|-- SingleThreadCache : impl\n    Cache <|-- MultiThreadCache : impl\n    SingleThreadCache --> RefCell : uses\n    MultiThreadCache --> Mutex : uses\n    CacheStats --> RefCell : uses\n```\n\n```rust\nuse std::collections::HashMap;\nuse std::sync::Mutex;\nuse std::cell::RefCell;\nuse std::hash::Hash;\n\ntrait Cache<K, V> {\n    fn get(&self, key: &K) -> Option<V>;\n    fn insert(&self, key: K, value: V);\n    fn remove(&self, key: &K) -> Option<V>;\n    fn clear(&self);\n}\n\n// Single-threaded cache using RefCell\nstruct SingleThreadCache<K, V> {\n    data: RefCell<HashMap<K, V>>,\n    max_size: usize,\n    stats: CacheStats,\n}\n\nimpl<K: Hash + Eq, V: Clone> SingleThreadCache<K, V> {\n    fn new(max_size: usize) -> Self {\n        SingleThreadCache {\n            data: RefCell::new(HashMap::new()),\n            max_size,\n            stats: CacheStats::new(),\n        }\n    }\n}\n\nimpl<K: Hash + Eq, V: Clone> Cache<K, V> for SingleThreadCache<K, V> {\n    fn get(&self, key: &K) -> Option<V> {\n        let data = self.data.borrow();\n        match data.get(key) {\n            Some(value) => {\n                self.stats.record_hit();\n                Some(value.clone())\n            }\n            None => {\n                self.stats.record_miss();\n                None\n            }\n        }\n    }\n    \n    fn insert(&self, key: K, value: V) {\n        let mut data = self.data.borrow_mut();\n        if data.len() >= self.max_size {\n            // Simple eviction: remove first entry\n            if let Some(first_key) = data.keys().next().cloned() {\n                data.remove(&first_key);\n            }\n        }\n        data.insert(key, value);\n    }\n    \n    fn remove(&self, key: &K) -> Option<V> {\n        self.data.borrow_mut().remove(key)\n    }\n    \n    fn clear(&self) {\n        self.data.borrow_mut().clear();\n    }\n}\n\n// Multi-threaded cache using Mutex\nstruct MultiThreadCache<K, V> {\n    data: Mutex<HashMap<K, V>>,\n    max_size: usize,\n}\n\nimpl<K: Hash + Eq, V: Clone> MultiThreadCache<K, V> {\n    fn new(max_size: usize) -> Self {\n        MultiThreadCache {\n            data: Mutex::new(HashMap::new()),\n            max_size,\n        }\n    }\n}\n\nimpl<K: Hash + Eq, V: Clone> Cache<K, V> for MultiThreadCache<K, V> {\n    fn get(&self, key: &K) -> Option<V> {\n        let data = self.data.lock().unwrap();\n        data.get(key).cloned()\n    }\n    \n    fn insert(&self, key: K, value: V) {\n        let mut data = self.data.lock().unwrap();\n        if data.len() >= self.max_size {\n            if let Some(first_key) = data.keys().next().cloned() {\n                data.remove(&first_key);\n            }\n        }\n        data.insert(key, value);\n    }\n    \n    fn remove(&self, key: &K) -> Option<V> {\n        self.data.lock().unwrap().remove(key)\n    }\n    \n    fn clear(&self) {\n        self.data.lock().unwrap().clear();\n    }\n}\n\nstruct CacheStats {\n    hits: RefCell<u64>,\n    misses: RefCell<u64>,\n}\n\nimpl CacheStats {\n    fn new() -> Self {\n        CacheStats {\n            hits: RefCell::new(0),\n            misses: RefCell::new(0),\n        }\n    }\n    \n    fn record_hit(&self) {\n        *self.hits.borrow_mut() += 1;\n    }\n    \n    fn record_miss(&self) {\n        *self.misses.borrow_mut() += 1;\n    }\n    \n    fn get_stats(&self) -> (u64, u64) {\n        (*self.hits.borrow(), *self.misses.borrow())\n    }\n}\n```"
  },
  {
    "instruction": "Design a class diagram for trait objects with dynamic dispatch for a plugin system",
    "output": "```mermaid\nclassDiagram\n    class Plugin {\n        <<interface>>\n        +name(&self) &str\n        +version(&self) &str\n        +execute(&self, input: &str) Result~String, PluginError~\n        +initialize(&mut self) Result~(), PluginError~\n        +cleanup(&mut self)\n    }\n    \n    class TextProcessorPlugin {\n        +name: String\n        +initialized: bool\n        +new() TextProcessorPlugin\n    }\n    \n    class DataValidatorPlugin {\n        +name: String\n        +rules: Vec~ValidationRule~\n        +new() DataValidatorPlugin\n    }\n    \n    class PluginManager {\n        +plugins: Vec~Box~dyn Plugin~~\n        +new() PluginManager\n        +register_plugin(&mut self, plugin: Box~dyn Plugin~)\n        +execute_all(&mut self, input: &str) Vec~Result~String, PluginError~~\n        +get_plugin_info(&self) Vec~PluginInfo~\n    }\n    \n    class PluginError {\n        +message: String\n        +plugin_name: String\n        +new(message: String, plugin_name: String) PluginError\n    }\n    \n    class PluginInfo {\n        +name: String\n        +version: String\n    }\n    \n    Plugin <|-- TextProcessorPlugin : impl\n    Plugin <|-- DataValidatorPlugin : impl\n    PluginManager --> Plugin : uses\n    Plugin --> PluginError : returns\n    PluginManager --> PluginInfo : creates\n```\n\n```rust\nuse std::fmt;\n\n#[derive(Debug)]\nstruct PluginError {\n    message: String,\n    plugin_name: String,\n}\n\nimpl PluginError {\n    fn new(message: String, plugin_name: String) -> Self {\n        PluginError { message, plugin_name }\n    }\n}\n\nimpl fmt::Display for PluginError {\n    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {\n        write!(f, \"Plugin '{}': {}\", self.plugin_name, self.message)\n    }\n}\n\nimpl std::error::Error for PluginError {}\n\ntrait Plugin {\n    fn name(&self) -> &str;\n    fn version(&self) -> &str;\n    fn execute(&self, input: &str) -> Result<String, PluginError>;\n    fn initialize(&mut self) -> Result<(), PluginError>;\n    fn cleanup(&mut self);\n}\n\nstruct TextProcessorPlugin {\n    name: String,\n    initialized: bool,\n}\n\nimpl TextProcessorPlugin {\n    fn new() -> Self {\n        TextProcessorPlugin {\n            name: \"TextProcessor\".to_string(),\n            initialized: false,\n        }\n    }\n}\n\nimpl Plugin for TextProcessorPlugin {\n    fn name(&self) -> &str {\n        &self.name\n    }\n    \n    fn version(&self) -> &str {\n        \"1.0.0\"\n    }\n    \n    fn execute(&self, input: &str) -> Result<String, PluginError> {\n        if !self.initialized {\n            return Err(PluginError::new(\n                \"Plugin not initialized\".to_string(),\n                self.name.clone(),\n            ));\n        }\n        Ok(input.to_uppercase())\n    }\n    \n    fn initialize(&mut self) -> Result<(), PluginError> {\n        self.initialized = true;\n        println!(\"TextProcessor plugin initialized\");\n        Ok(())\n    }\n    \n    fn cleanup(&mut self) {\n        self.initialized = false;\n        println!(\"TextProcessor plugin cleaned up\");\n    }\n}\n\nstruct ValidationRule {\n    name: String,\n    pattern: String,\n}\n\nstruct DataValidatorPlugin {\n    name: String,\n    rules: Vec<ValidationRule>,\n}\n\nimpl DataValidatorPlugin {\n    fn new() -> Self {\n        DataValidatorPlugin {\n            name: \"DataValidator\".to_string(),\n            rules: Vec::new(),\n        }\n    }\n}\n\nimpl Plugin for DataValidatorPlugin {\n    fn name(&self) -> &str {\n        &self.name\n    }\n    \n    fn version(&self) -> &str {\n        \"2.1.0\"\n    }\n    \n    fn execute(&self, input: &str) -> Result<String, PluginError> {\n        if input.is_empty() {\n            return Err(PluginError::new(\n                \"Input cannot be empty\".to_string(),\n                self.name.clone(),\n            ));\n        }\n        Ok(format!(\"Validated: {}\", input))\n    }\n    \n    fn initialize(&mut self) -> Result<(), PluginError> {\n        self.rules.push(ValidationRule {\n            name: \"non_empty\".to_string(),\n            pattern: \".+\".to_string(),\n        });\n        println!(\"DataValidator plugin initialized with {} rules\", self.rules.len());\n        Ok(())\n    }\n    \n    fn cleanup(&mut self) {\n        self.rules.clear();\n        println!(\"DataValidator plugin cleaned up\");\n    }\n}\n\n#[derive(Clone)]\nstruct PluginInfo {\n    name: String,\n    version: String,\n}\n\nstruct PluginManager {\n    plugins: Vec<Box<dyn Plugin>>,\n}\n\nimpl PluginManager {\n    fn new() -> Self {\n        PluginManager {\n            plugins: Vec::new(),\n        }\n    }\n    \n    fn register_plugin(&mut self, mut plugin: Box<dyn Plugin>) {\n        if let Err(e) = plugin.initialize() {\n            eprintln!(\"Failed to initialize plugin: {}\", e);\n            return;\n        }\n        self.plugins.push(plugin);\n    }\n    \n    fn execute_all(&self, input: &str) -> Vec<Result<String, PluginError>> {\n        self.plugins\n            .iter()\n            .map(|plugin| plugin.execute(input))\n            .collect()\n    }\n    \n    fn get_plugin_info(&self) -> Vec<PluginInfo> {\n        self.plugins\n            .iter()\n            .map(|plugin| PluginInfo {\n                name: plugin.name().to_string(),\n                version: plugin.version().to_string(),\n            })\n            .collect()\n    }\n}\n```"
  },
  {
    "instruction": "Create a class diagram showing the newtype pattern for different units of measurement with type safety",
    "output": "```mermaid\nclassDiagram\n    class Meters {\n        -value: f64\n        +new(value: f64) Meters\n        +value(&self) f64\n        +to_kilometers(&self) Kilometers\n        +to_feet(&self) Feet\n    }\n    \n    class Kilometers {\n        -value: f64\n        +new(value: f64) Kilometers\n        +value(&self) f64\n        +to_meters(&self) Meters\n    }\n    \n    class Feet {\n        -value: f64\n        +new(value: f64) Feet\n        +value(&self) f64\n        +to_meters(&self) Meters\n    }\n    \n    class Temperature~U~ {\n        -value: f64\n        +new(value: f64) Temperature~U~\n        +value(&self) f64\n    }\n    \n    class Celsius {\n        <<unit>>\n    }\n    \n    class Fahrenheit {\n        <<unit>>\n    }\n    \n    class Kelvin {\n        <<unit>>\n    }\n    \n    class UnitConversions {\n        <<trait>>\n        +to_celsius(self) Temperature~Celsius~\n        +to_fahrenheit(self) Temperature~Fahrenheit~\n        +to_kelvin(self) Temperature~Kelvin~\n    }\n    \n    Temperature --> Celsius : parameterized\n    Temperature --> Fahrenheit : parameterized\n    Temperature --> Kelvin : parameterized\n    UnitConversions <|-- Temperature : impl\n```\n\n```rust\nuse std::fmt;\nuse std::ops::{Add, Sub, Mul, Div};\n\n// Distance units\n#[derive(Debug, Clone, Copy, PartialEq, PartialOrd)]\nstruct Meters(f64);\n\n#[derive(Debug, Clone, Copy, PartialEq, PartialOrd)]\nstruct Kilometers(f64);\n\n#[derive(Debug, Clone, Copy, PartialEq, PartialOrd)]\nstruct Feet(f64);\n\nimpl Meters {\n    fn new(value: f64) -> Self {\n        Meters(value)\n    }\n    \n    fn value(&self) -> f64 {\n        self.0\n    }\n    \n    fn to_kilometers(&self) -> Kilometers {\n        Kilometers(self.0 / 1000.0)\n    }\n    \n    fn to_feet(&self) -> Feet {\n        Feet(self.0 * 3.28084)\n    }\n}\n\nimpl Kilometers {\n    fn new(value: f64) -> Self {\n        Kilometers(value)\n    }\n    \n    fn value(&self) -> f64 {\n        self.0\n    }\n    \n    fn to_meters(&self) -> Meters {\n        Meters(self.0 * 1000.0)\n    }\n}\n\nimpl Feet {\n    fn new(value: f64) -> Self {\n        Feet(value)\n    }\n    \n    fn value(&self) -> f64 {\n        self.0\n    }\n    \n    fn to_meters(&self) -> Meters {\n        Meters(self.0 / 3.28084)\n    }\n}\n\n// Temperature units with phantom types\nuse std::marker::PhantomData;\n\n#[derive(Debug, Clone, Copy, PartialEq, PartialOrd)]\nstruct Temperature<U> {\n    value: f64,\n    _unit: PhantomData<U>,\n}\n\n// Unit marker types\nstruct Celsius;\nstruct Fahrenheit;\nstruct Kelvin;\n\nimpl<U> Temperature<U> {\n    fn new(value: f64) -> Self {\n        Temperature {\n            value,\n            _unit: PhantomData,\n        }\n    }\n    \n    fn value(&self) -> f64 {\n        self.value\n    }\n}\n\ntrait UnitConversions<U> {\n    fn to_celsius(self) -> Temperature<Celsius>;\n    fn to_fahrenheit(self) -> Temperature<Fahrenheit>;\n    fn to_kelvin(self) -> Temperature<Kelvin>;\n}\n\nimpl UnitConversions<Celsius> for Temperature<Celsius> {\n    fn to_celsius(self) -> Temperature<Celsius> {\n        self\n    }\n    \n    fn to_fahrenheit(self) -> Temperature<Fahrenheit> {\n        Temperature::new(self.value * 9.0 / 5.0 + 32.0)\n    }\n    \n    fn to_kelvin(self) -> Temperature<Kelvin> {\n        Temperature::new(self.value + 273.15)\n    }\n}\n\nimpl UnitConversions<Fahrenheit> for Temperature<Fahrenheit> {\n    fn to_celsius(self) -> Temperature<Celsius> {\n        Temperature::new((self.value - 32.0) * 5.0 / 9.0)\n    }\n    \n    fn to_fahrenheit(self) -> Temperature<Fahrenheit> {\n        self\n    }\n    \n    fn to_kelvin(self) -> Temperature<Kelvin> {\n        Temperature::new((self.value - 32.0) * 5.0 / 9.0 + 273.15)\n    }\n}\n\nimpl UnitConversions<Kelvin> for Temperature<Kelvin> {\n    fn to_celsius(self) -> Temperature<Celsius> {\n        Temperature::new(self.value - 273.15)\n    }\n    \n    fn to_fahrenheit(self) -> Temperature<Fahrenheit> {\n        Temperature::new((self.value - 273.15) * 9.0 / 5.0 + 32.0)\n    }\n    \n    fn to_kelvin(self) -> Temperature<Kelvin> {\n        self\n    }\n}\n\n// Implement arithmetic operations for same units\nimpl Add for Meters {\n    type Output = Self;\n    \n    fn add(self, other: Self) -> Self {\n        Meters(self.0 + other.0)\n    }\n}\n\nimpl<U> Add for Temperature<U> {\n    type Output = Self;\n    \n    fn add(self, other: Self) -> Self {\n        Temperature::new(self.value + other.value)\n    }\n}\n```"
  },
  {
    "instruction": "Show a class diagram for a builder pattern with typestate for database connection configuration",
    "output": "```mermaid\nclassDiagram\n    class DatabaseBuilder~S~ {\n        +host: Option~String~\n        +port: Option~u16~\n        +database: Option~String~\n        +username: Option~String~\n        +password: Option~String~\n        +ssl_enabled: bool\n        +connection_timeout: Duration\n    }\n    \n    class NoHost {\n        <<state>>\n    }\n    \n    class HasHost {\n        <<state>>\n    }\n    \n    class NoDatabase {\n        <<state>>\n    }\n    \n    class HasDatabase {\n        <<state>>\n    }\n    \n    class DatabaseConnection {\n        +host: String\n        +port: u16\n        +database: String\n        +username: String\n        +password: String\n        +ssl_enabled: bool\n        +connection_timeout: Duration\n        +connect(&self) Result~Connection, ConnectionError~\n    }\n    \n    class Connection {\n        +is_connected: bool\n        +execute_query(&self, query: &str) Result~QueryResult, DatabaseError~\n        +close(&mut self)\n    }\n    \n    class ConnectionError {\n        +message: String\n        +error_type: ErrorType\n    }\n    \n    DatabaseBuilder --> NoHost : initial\n    DatabaseBuilder --> HasHost : after host\n    DatabaseBuilder --> NoDatabase : initial  \n    DatabaseBuilder --> HasDatabase : after database\n    DatabaseBuilder --> DatabaseConnection : build\n    DatabaseConnection --> Connection : creates\n    DatabaseConnection --> ConnectionError : returns\n```\n\n```rust\nuse std::marker::PhantomData;\nuse std::time::Duration;\n\n// State types for typestate pattern\nstruct NoHost;\nstruct HasHost;\nstruct NoDatabase;\nstruct HasDatabase;\n\n// Compound states using tuples\ntype InitialState = (NoHost, NoDatabase);\ntype HasHostNoDb = (HasHost, NoDatabase);\ntype NoHostHasDb = (NoHost, HasDatabase);\ntype CompleteState = (HasHost, HasDatabase);\n\nstruct DatabaseBuilder<S> {\n    host: Option<String>,\n    port: Option<u16>,\n    database: Option<String>,\n    username: Option<String>,\n    password: Option<String>,\n    ssl_enabled: bool,\n    connection_timeout: Duration,\n    _state: PhantomData<S>,\n}\n\n// Constructor - starts in initial state\nimpl DatabaseBuilder<InitialState> {\n    fn new() -> Self {\n        DatabaseBuilder {\n            host: None,\n            port: None,\n            database: None,\n            username: None,\n            password: None,\n            ssl_enabled: false,\n            connection_timeout: Duration::from_secs(30),\n            _state: PhantomData,\n        }\n    }\n    \n    fn host(mut self, host: String) -> DatabaseBuilder<HasHostNoDb> {\n        self.host = Some(host);\n        DatabaseBuilder {\n            host: self.host,\n            port: self.port,\n            database: self.database,\n            username: self.username,\n            password: self.password,\n            ssl_enabled: self.ssl_enabled,\n            connection_timeout: self.connection_timeout,\n            _state: PhantomData,\n        }\n    }\n    \n    fn database(mut self, database: String) -> DatabaseBuilder<NoHostHasDb> {\n        self.database = Some(database);\n        DatabaseBuilder {\n            host: self.host,\n            port: self.port,\n            database: self.database,\n            username: self.username,\n            password: self.password,\n            ssl_enabled: self.ssl_enabled,\n            connection_timeout: self.connection_timeout,\n            _state: PhantomData,\n        }\n    }\n}\n\n// Implementations for partial states\nimpl DatabaseBuilder<HasHostNoDb> {\n    fn database(mut self, database: String) -> DatabaseBuilder<CompleteState> {\n        self.database = Some(database);\n        DatabaseBuilder {\n            host: self.host,\n            port: self.port,\n            database: self.database,\n            username: self.username,\n            password: self.password,\n            ssl_enabled: self.ssl_enabled,\n            connection_timeout: self.connection_timeout,\n            _state: PhantomData,\n        }\n    }\n}\n\nimpl DatabaseBuilder<NoHostHasDb> {\n    fn host(mut self, host: String) -> DatabaseBuilder<CompleteState> {\n        self.host = Some(host);\n        DatabaseBuilder {\n            host: self.host,\n            port: self.port,\n            database: self.database,\n            username: self.username,\n            password: self.password,\n            ssl_enabled: self.ssl_enabled,\n            connection_timeout: self.connection_timeout,\n            _state: PhantomData,\n        }\n    }\n}\n\n// Optional configuration methods available in any state\nimpl<S> DatabaseBuilder<S> {\n    fn port(mut self, port: u16) -> Self {\n        self.port = Some(port);\n        self\n    }\n    \n    fn username(mut self, username: String) -> Self {\n        self.username = Some(username);\n        self\n    }\n    \n    fn password(mut self, password: String) -> Self {\n        self.password = Some(password);\n        self\n    }\n    \n    fn ssl_enabled(mut self, enabled: bool) -> Self {\n        self.ssl_enabled = enabled;\n        self\n    }\n    \n    fn connection_timeout(mut self, timeout: Duration) -> Self {\n        self.connection_timeout = timeout;\n        self\n    }\n}\n\n// Build method only available when all required fields are set\nimpl DatabaseBuilder<CompleteState> {\n    fn build(self) -> DatabaseConnection {\n        DatabaseConnection {\n            host: self.host.unwrap(),\n            port: self.port.unwrap_or(5432),\n            database: self.database.unwrap(),\n            username: self.username.unwrap_or_else(|| \"postgres\".to_string()),\n            password: self.password.unwrap_or_else(|| String::new()),\n            ssl_enabled: self.ssl_enabled,\n            connection_timeout: self.connection_timeout,\n        }\n    }\n}\n\n#[derive(Debug)]\nstruct DatabaseConnection {\n    host: String,\n    port: u16,\n    database: String,\n    username: String,\n    password: String,\n    ssl_enabled: bool,\n    connection_timeout: Duration,\n}\n\n#[derive(Debug)]\nenum ErrorType {\n    NetworkError,\n    AuthenticationError,\n    DatabaseNotFound,\n    Timeout,\n}\n\n#[derive(Debug)]\nstruct ConnectionError {\n    message: String,\n    error_type: ErrorType,\n}\n\n#[derive(Debug)]\nstruct Connection {\n    is_connected: bool,\n}\n\n#[derive(Debug)]\nstruct QueryResult {\n    rows_affected: usize,\n}\n\n#[derive(Debug)]\nstruct DatabaseError {\n    message: String,\n}\n\nimpl DatabaseConnection {\n    fn connect(&self) -> Result<Connection, ConnectionError> {\n        // Simulate connection logic\n        println!(\"Connecting to {}:{}/{}\", self.host, self.port, self.database);\n        Ok(Connection {\n            is_connected: true,\n        })\n    }\n}\n\nimpl Connection {\n    fn execute_query(&self, query: &str) -> Result<QueryResult, DatabaseError> {\n        if !self.is_connected {\n            return Err(DatabaseError {\n                message: \"Not connected to database\".to_string(),\n            });\n        }\n        println!(\"Executing query: {}\", query);\n        Ok(QueryResult { rows_affected: 1 })\n    }\n    \n    fn close(&mut self) {\n        self.is_connected = false;\n        println!(\"Connection closed\");\n    }\n}\n```"
  },
  {
    "instruction": "Design a class diagram for comprehensive Result-based error handling with custom error types and error chaining",
    "output": "```mermaid\nclassDiagram\n    class AppError {\n        <<enumeration>>\n        +Network(NetworkError)\n        +Validation(ValidationError)\n        +Database(DatabaseError)\n        +Config(ConfigError)\n    }\n    \n    class NetworkError {\n        +url: String\n        +status_code: Option~u16~\n        +message: String\n        +source: Option~Box~dyn Error~~\n    }\n    \n    class ValidationError {\n        +field: String\n        +value: String\n        +constraint: String\n        +source: Option~Box~dyn Error~~\n    }\n    \n    class DatabaseError {\n        +operation: String\n        +table: Option~String~\n        +message: String\n        +source: Option~Box~dyn Error~~\n    }\n    \n    class ConfigError {\n        +config_file: String\n        +key: Option~String~\n        +message: String\n    }\n    \n    class ErrorHandler {\n        +handle_error(error: &AppError)\n        +log_error(error: &AppError)\n        +user_friendly_message(error: &AppError) String\n        +should_retry(error: &AppError) bool\n    }\n    \n    class UserService {\n        +create_user(data: UserData) Result~User, AppError~\n        +get_user(id: u64) Result~User, AppError~\n        +validate_user_data(data: &UserData) Result~(), AppError~\n    }\n    \n    AppError --> NetworkError : contains\n    AppError --> ValidationError : contains\n    AppError --> DatabaseError : contains\n    AppError --> ConfigError : contains\n    ErrorHandler --> AppError : handles\n    UserService --> AppError : returns\n```\n\n```rust\nuse std::fmt;\nuse std::error::Error;\n\n// Main application error enum\n#[derive(Debug)]\nenum AppError {\n    Network(NetworkError),\n    Validation(ValidationError),\n    Database(DatabaseError),\n    Config(ConfigError),\n}\n\n#[derive(Debug)]\nstruct NetworkError {\n    url: String,\n    status_code: Option<u16>,\n    message: String,\n    source: Option<Box<dyn Error + Send + Sync>>,\n}\n\n#[derive(Debug)]\nstruct ValidationError {\n    field: String,\n    value: String,\n    constraint: String,\n    source: Option<Box<dyn Error + Send + Sync>>,\n}\n\n#[derive(Debug)]\nstruct DatabaseError {\n    operation: String,\n    table: Option<String>,\n    message: String,\n    source: Option<Box<dyn Error + Send + Sync>>,\n}\n\n#[derive(Debug)]\nstruct ConfigError {\n    config_file: String,\n    key: Option<String>,\n    message: String,\n}\n\n// Implement Display for all error types\nimpl fmt::Display for AppError {\n    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {\n        match self {\n            AppError::Network(e) => write!(f, \"Network error: {}\", e),\n            AppError::Validation(e) => write!(f, \"Validation error: {}\", e),\n            AppError::Database(e) => write!(f, \"Database error: {}\", e),\n            AppError::Config(e) => write!(f, \"Configuration error: {}\", e),\n        }\n    }\n}\n\nimpl fmt::Display for NetworkError {\n    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {\n        write!(f, \"Failed to access {}: {}\", self.url, self.message)\n    }\n}\n\nimpl fmt::Display for ValidationError {\n    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {\n        write!(f, \"Invalid value '{}' for field '{}': {}\", \n               self.value, self.field, self.constraint)\n    }\n}\n\nimpl fmt::Display for DatabaseError {\n    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {\n        match &self.table {\n            Some(table) => write!(f, \"Database operation '{}' failed on table '{}': {}\", \n                                 self.operation, table, self.message),\n            None => write!(f, \"Database operation '{}' failed: {}\", \n                          self.operation, self.message),\n        }\n    }\n}\n\nimpl fmt::Display for ConfigError {\n    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {\n        match &self.key {\n            Some(key) => write!(f, \"Configuration error in '{}' for key '{}': {}\", \n                               self.config_file, key, self.message),\n            None => write!(f, \"Configuration error in '{}': {}\", \n                          self.config_file, self.message),\n        }\n    }\n}\n\n// Implement Error trait\nimpl Error for AppError {\n    fn source(&self) -> Option<&(dyn Error + 'static)> {\n        match self {\n            AppError::Network(e) => e.source.as_ref().map(|e| e.as_ref() as &dyn Error),\n            AppError::Validation(e) => e.source.as_ref().map(|e| e.as_ref() as &dyn Error),\n            AppError::Database(e) => e.source.as_ref().map(|e| e.as_ref() as &